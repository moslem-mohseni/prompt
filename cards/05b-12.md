---
id: 05b-12
title: جدول اکسل با فرمول
category: data
type: prompt
tool: sheets
privacy: yellow
tags: [اکسل, xlsx, فرمول, google sheets, جدول]
fields:
  - key: columns
    label: ستون‌ها
    type: textarea
    required: true
  - key: formula_cols
    label: ستون‌های محاسباتی (باید فرمول باشند)
    required: true
  - key: rows
    label: داده‌ی اولیه (اختیاری)
    type: textarea
examples:
  reza:
    columns: هزینه‌ی کالا، هزینه‌ی بسته‌بندی، سهم ارسال، هزینه‌ی متغیر هر بسته، قیمت فروش، سود هر بسته، درصد حاشیه، هزینه‌ی ثابت، نقطه‌ی سربه‌سر
    formula_cols: هزینه‌ی متغیر هر بسته، سود هر بسته، درصد حاشیه، نقطه‌ی سربه‌سر
    rows: کالا ۴۲۰، بسته‌بندی ۶۰، ارسال ۴۰، قیمت ۷۸۰، هزینه‌ی ثابت ۶۵۰۰ (هزار تومان)
---

## کاربرد

فایل اکسلی که ستون‌های محاسباتی‌اش فرمول‌اند.

## قالب

```
یک فایل اکسل بساز با ستون‌های: {{columns}}.
{{#rows}}داده‌ی اولیه: {{rows}}
{{/rows}}ستون‌های {{formula_cols}} باید فرمول باشند، نه عدد ثابت، تا اگر یک
عدد را عوض کردم بقیه خودکار حساب شوند. فرمول هر ستون را در یک خط توضیح بده.
```
