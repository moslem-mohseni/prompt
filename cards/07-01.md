---
id: 07-01
title: پرامپت آزمون برای مقایسه‌ی ابزارها
category: think
type: prompt
tool: compare
privacy: green
tags: [آزمون یکسان, مقایسه‌ی کور, arena, battle, پرامپت سبز]
fields:
  - key: task
    label: کار کارت کیس
    type: textarea
    required: true
  - key: context
    label: زمینه (فقط داده‌ی ساختگی یا عمومی)
    type: textarea
    required: true
    anon: true
    help: این پرامپت در Arena اجرا می‌شود و عمومی فرض می‌شود. نام، عدد واقعی فروش و هر داده‌ی حساس را با نمونه‌ی ساختگی عوض کنید.
  - key: constraints
    label: قیدها، هر کدام با دلیل
    type: textarea
    required: true
  - key: format
    label: قالب خروجی
    required: true
examples:
  mina:
    task: "تحلیل یک خطای منطقی در کوئری SQL و بازنویسی بهینه‌تر آن"
    context: "یک جدول سفارش‌ها با ۱۰ میلیون ردیف داریم که کوئری فعلی به دلیل استفاده از زیرکوئری در بند WHERE زمان اجرای بیش از ۴ دقیقه دارد."
    constraints: "کد نهایی فقط با استفاده از توابع تحلیلی (Window Functions) بدون حلقه تکرار نوشته شود؛ چرایی بهبود کارایی در دو بند توضیح داده شود."
    format: "بلوک کد اصلاح‌شده همراه با جدول مقایسه هزینه محاسباتی."
---

## کاربرد

یک پرامپت ثابت برای مقایسه‌ی ابزارها روی کار خودتان.

## قالب

```
کار: {{task}}
زمینه:
{{context}}
قیدها: {{constraints}}
قالب خروجی: {{format}}
```
